/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // No theme.extend here in v4 – move to CSS
  plugins: [], // Add if needed
};